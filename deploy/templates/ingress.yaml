apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress
  annotations:
    kubernetes.io/ingress.class: "nginx"
spec:
  rules:
    - host: springapp
      http:
        paths:
          - backend:
              serviceName: api
              servicePort: 80
            path: /